#ifndef TOWERVM_MEMORY_H
#define TOWERVM_MEMORY_H

#include <cstddef>
#include <cstdint>

typedef size_t Addr;

// TODO: Make this a tagged union that includes data type
typedef uint32_t Word;

typedef int32_t IntWord;

typedef float FloatWord;

#endif // TOWERVM_MEMORY_H
